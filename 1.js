// 1. Declare a function that takes 4 arguments, and adds them all together, and returns the sum.

function add(a, b, c, d) {
    const total = a + b + c + d;
    return total

}

console.log(add(1, 7, 3, 10)); //expecting 21